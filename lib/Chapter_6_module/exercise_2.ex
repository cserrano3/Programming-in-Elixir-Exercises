defmodule CH_6_Exercise_2 do
  # def recursive_sum(n) do
  #  def sum(1), do: 1
  #  def sum(n), do: n + (n - 1)
  #end

  #def greatest_common_divisor do
  #  def gcd(x,0), do: x
  #  def gcd(x, y), do: gcd(y, rem(x,y))
  #end
end
